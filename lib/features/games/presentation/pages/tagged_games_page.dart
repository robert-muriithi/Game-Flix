import 'package:flutter/material.dart';

class TagsGamesPage extends StatelessWidget {
  const TagsGamesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
